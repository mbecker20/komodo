[gen-ts-types]
description = "generates typescript types for the ts client"
path = "."
cmd = "node ./client/core/ts/generate_types.mjs"

[start-frontend]
path = "frontend"
cmd = "yarn dev"

[build-frontend]
path = "frontend"
cmd = "yarn build"

[monrun-sync]
path = "bin/monrun"
cmd = "cargo run -- sync"