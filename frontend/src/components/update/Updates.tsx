import { Component } from "solid-js";
import { UpdateTarget } from "../../types";

const Updates: Component<{ targets: UpdateTarget[] }> = (p) => {
	return (
		<div>
			
		</div>
	);
}

export default Updates;